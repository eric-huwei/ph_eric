<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.week08.mapper.OrderMapper" >
    <resultMap id="OrderResultMapper" type="com.week08.entity.Order" >
        <id column="order_id" property="orderId" jdbcType="INTEGER" />
        <id column="user_id" property="userId" jdbcType="INTEGER" />
    </resultMap>

    <select id="getOrderInfo" resultMap="OrderResultMapper">
      select * from t_order where order_id = #{orderId, jdbcType=INTEGER} and user_id = #{userId, jdbcType=INTEGER}
    </select>
</mapper>